DROP TABLE IF EXISTS products;

CREATE TABLE IF NOT EXISTS products (
    identifier BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY, 
    name 	   VARCHAR NOT NULL,
    price      DOUBLE NOT NULL
);
